import { Hero as ExistingHero } from "@/components/sections/hero"

export const Hero = () => {
  // Template-style component wrapper; we’ll migrate JSX + copy into this folder next.
  return <ExistingHero />
}

